import "./styles/main.scss";

import { startButton } from "./features/home";

init();

/**
 * Initializes the application by setting up the start button functionality.
 */
function init() {
  startButton();
}
